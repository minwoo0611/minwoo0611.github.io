# minwoo0611.github.io

Personal academic website for Minwoo Jung, built with the al-folio Jekyll theme.

## Local Build

```bash
bundle exec jekyll build
```

The GitHub Actions workflow in `.github/workflows/deploy.yml` builds the site and deploys the generated `_site` output after changes are pushed to `master` or `main`.
